services:
  baos:
    build: .
    image: baos:latest
    container_name: baos
    ports:
      - "6080:6080"
      - "5900:5900"
    stdin_open: true
    tty: true
    volumes:
      - baos-data:/baos

volumes:
  baos-data:
